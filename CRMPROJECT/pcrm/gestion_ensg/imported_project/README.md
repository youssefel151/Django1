Imported reference files from the original Scolarite/G.ensg Django project.

The active integration is the `gestion_ensg` app mounted in CRMPROJECT at `/scolarite/`.
These files are preserved for reference/data recovery only and are not used by Django at runtime.
